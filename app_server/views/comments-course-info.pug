extends layout

include _includes/sharedHTMLfunctions

block content
  .row
    .col-12.col-md-6
        a.btn.btn-primary.float-right(href=`/courses/${course._id}`) Back To Course
  .row.banner
    .col-12
      h1 #{pageHeader.title} | Course Details 
  .row
    .col-12.col-lg-9
      .row
        .col-12.col-md-6
          a.btn.btn-primary.float-right.button(href=`/courses/${course._id}/update`) Update Course
          p  #{course.name} - #{course.description}
          small &nbsp;
              +outputRating(course.rating)
        .col-12.col-md-6.location-map
          .card.card-primary
            .card-block
              h2.card-title Course Attended
              img.img-fluid.rounded(src=`/images/ProductionLine.jpg`)
      .row
        .col-12
          //- p !{(comment.comment).replace(/\n/g, '<br/>')}
          .card.card-primary.review-card
            .card-block
              a.btn.btn-primary.float-right(href=`/courses/${course._id}/comments/new`) Add Comment
              h2.card-title Course Comments
              each comment in course.comments
                .row.review
                  .col-12.no-gutters.review-header
                    span.reviewAuthor.push 
                      +formatDateTime(comment.createdOn)
                  .col-12
                    .card-block
                        p.address= comment.comment
    .col-12.col-lg-3
      p.lead #{sidebar.context}
      p= sidebar.callToAction